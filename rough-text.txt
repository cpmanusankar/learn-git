git init
git status

git add name / git add .

git commit -m "...."

git log

git add .
git commit -m "version 1" --amend

git reset config.js

git checkout -- config.js / .  

git checkout commit-hash

git log --all


git log --all --graph

git checkout [version1#]  


alias
git status --> git s
git config --global alias.s "status"


remove git:
rm -rf .git 


git remote add origin 

git remote remove origin

git push origin 

 git config --global credential.username "cpmanusankar"

git push origin master


**************************


git push origin master

git push origin master --set-upstream
: git push

git commit --amend -m "version 12"

git push origin master -f (force)

**clone

git clone url folder-name

git fetch

git pull origin master

